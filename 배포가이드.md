# 🐾 PetCTT 실시간 접속자 위젯 - 배포 가이드

## 만들어진 파일들

| 파일 | 용도 |
|------|------|
| `worker-index.js` | Cloudflare Worker 코드 (접속자 추적 서버) |
| `wrangler.jsonc` | Worker 배포 설정 파일 |
| `widget-embed.html` | petctt.com에 삽입할 위젯 코드 |

---

## 📌 배포 순서 (3단계)

### 1단계: Cloudflare Worker 배포

**방법 A: Cloudflare 대시보드에서 직접 (가장 쉬움)**

1. dash.cloudflare.com → Workers & Pages → 개요
2. "Worker 만들기" 클릭
3. 이름: `petctt-visitors`
4. 코드 편집기에서 `worker-index.js` 내용 붙여넣기
5. "설정" 탭 → KV 네임스페이스 바인딩 추가:
   - 변수 이름: `VISITORS`
   - KV 네임스페이스: `petctt-visitors` (ID: `8768dc606a7842ebae9f7d59b84d2a2b`)
6. "배포" 클릭

**방법 B: Wrangler CLI (개발자용)**

```bash
# wrangler 설치
npm install -g wrangler

# 로그인
wrangler login

# 배포
cd petctt-visitor-counter
wrangler deploy
```

배포 후 Worker URL: `https://petctt-visitors.zeus1404.workers.dev`

---

### 2단계: petctt.com에 위젯 삽입

`widget-embed.html` 파일의 내용을 petctt.com의 `index.html` 등 모든 페이지의 `</body>` 바로 위에 삽입

⚠️ 중요: `WORKER_URL` 변수를 실제 Worker URL로 변경!

```javascript
const WORKER_URL = 'https://petctt-visitors.zeus1404.workers.dev';
```

---

### 3단계: GitHub에 push & 확인

```bash
git add .
git commit -m "🐾 실시간 접속자 위젯 추가"
git push
```

배포 후 petctt.com 접속 → 왼쪽 하단에 🐾 접속자 위젯 확인!

---

## ✅ 기능 요약

- 🟢 실시간 접속자 수 (5분 기준 활성 세션)
- 📊 페이지별 접속 현황 (클릭시 상세보기)
- 📈 오늘 총 방문자 수
- 🔄 60초마다 자동 업데이트
- 💫 깔끔한 다크 테마 위젯
- 📱 모바일 호환

## 이미 완료된 것

- ✅ GA4 연동 (활성 사용자 23명, Organic Search 25세션)
- ✅ KV 네임스페이스 생성 (ID: 8768dc606a7842ebae9f7d59b84d2a2b)
- ✅ Worker 코드 작성
- ✅ 위젯 UI 코드 작성
